<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.shibaura_it.sayo.se.reservationsystem.user" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".controller.MainActivity"
            android:label="@string/app_name_jp" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.TimeSelectActivity"
            android:label="@string/title_activity_time_select" >
        </activity>
        <activity
            android:name=".controller.ReserveCompleteActivity"
            android:label="@string/title_activity_reserve_complete" >
        </activity>
        <activity
            android:name=".controller.UserRegistActivity"
            android:label="@string/title_activity_user_regist" >
        </activity>
    </application>

</manifest>
